default:
    suites:
        comment_application_features:
            contexts_services:
                - 'tests.behat.context.application.comment_context'
                - 'tests.behat.context.application.security_context'

                - 'tests.behat.context.setup.group_context'
                - 'tests.behat.context.setup.idea_context'

                - 'tests.behat.context.transform.group_context'
                - 'tests.behat.context.transform.idea_context'
            filters:
                tags: '@comment && @application'