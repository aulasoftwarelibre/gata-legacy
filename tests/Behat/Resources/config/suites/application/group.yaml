default:
    suites:
        group_application_features:
            contexts_services:
                - 'tests.behat.context.application.group_context'
            filters:
                tags: '@group && @application'
